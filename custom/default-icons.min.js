/* Empty default icon pack — every icon is provided by lucide/icons.js (custom Lucide pack).
   The stock TinyMCE default pack is replaced by this placeholder on purpose: with the
   Lucide pack active, loading the original default pack throws, so it is neutralised here. */
tinymce.IconManager.add("default",{icons:{}});
